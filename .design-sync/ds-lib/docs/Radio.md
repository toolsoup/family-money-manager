---
category: Forms
---

# Radio

A single radio option with a gold-filled dot when selected. Group several by sharing a `name`.

```jsx
<Radio name="strategy" label="Avalanche — highest interest first" checked onChange={...} />
<Radio name="strategy" label="Snowball — smallest balance first" onChange={...} />
```
