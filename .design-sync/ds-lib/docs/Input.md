---
category: Forms
---

# Input

Single-line text input in the Midnight Gold style (surface fill, gold focus ring). Accepts all native input props (`type`, `placeholder`, `value`, `onChange`). Wrap in `Field` to add a label.

```jsx
<Input placeholder="Account nickname" />
```
