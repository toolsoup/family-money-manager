---
category: Data
---

# Meter

Thin progress / spending bar — gold gradient fill on a dark track. `value` is 0–100 (clamped). Use `muted` for secondary or over-budget bars.

```jsx
<Meter value={57} />
<Meter value={100} muted />
```
